
        document.addEventListener("DOMContentLoaded", () => {
            const url = window.location.href;
            const params = new URLSearchParams(new URL(url).search);
            const department = params.get("sub")
            function downloadFile(endpoint, filename) {
                fetch(endpoint, {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ department: department }) // غيّر القسم حسب الحاجة
                })
                    .then(response => {
                        if (!response.ok) throw new Error("خطأ في تحميل الملف");
                        return response.blob();
                    })
                    .then(blob => {
                        const link = document.createElement("a");
                        link.href = URL.createObjectURL(blob);
                        link.download = filename;
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);

                        setTimeout(() => {
                            let url = new URL("http://localhost:3000/Admine/Doctor/successDownload");

                            window.location = url;
                        }, 3000);
                    })
                    .catch(error => console.error("Error:", error));
            }

            document.querySelector(".bg-blue-500").addEventListener("click", () => {
                downloadFile("http://localhost:3000/Admine/Doctor/DownloadList", `${department}.pdf`);
            });
        });
        ///////////////////////////////////////////////////////
        document.getElementById("downloadWord").addEventListener("click", function () {
            const url = window.location.href;
            const params = new URLSearchParams(new URL(url).search);
            const department = params.get("sub")
            fetch("http://localhost:3000/Admine/Doctor/DownloadList", {
                method: "PATCH",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({ department: department }) // غيّر اسم القسم حسب الحاجة
            })
                .then(response => response.blob())
                .then(blob => {
                    const url = window.URL.createObjectURL(blob);
                    const a = document.createElement("a");
                    a.href = url;
                    a.download = `${department}.docx`;
                    document.body.appendChild(a);
                    a.click();
                    a.remove();
                    setTimeout(() => {
                        let url = new URL("https://7959-197-192-206-85.ngrok-free.app/Admine/Doctor/successDownload");

                        window.location = url;
                    }, 3000);
                })
                .catch(error => console.error("Error downloading file:", error));
        });

    </script>
</body>

</html> --}}




<!DOCTYPE html>
<html lang="en">

<head>
      <meta charset="UTF-8">
     <link rel="shortcut icon" href="/images/OIP.jpg" type="image/x-icon">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DownloadList</title>
    <link rel="stylesheet" href="/css/DownloadList.css">
</head>

<body>
    <section class="sec1">
        <img src="/Images/img1.jpg" alt="وصف الصورة" />

        <div class="div1">
            <h1 class="h1">Download Your Document</h1>
            <p class="p1">You can download the document in</p>
            <p class="p2">PDF or Word format as you prefer</p>
            <button class="btn1">Download as PDF</button>
            <button  id="downloadWord" class="btn2">Download as Word</button>
        </div>
    </section>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const url = window.location.href;
            const params = new URLSearchParams(new URL(url).search);
            const department = params.get("sub")
            function downloadFile(endpoint, filename) {
                fetch(endpoint, {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ department: department }) // غيّر القسم حسب الحاجة
                })
                    .then(response => {
                        if (!response.ok) throw new Error("خطأ في تحميل الملف");
                        return response.blob();
                    })
                    .then(blob => {
                        const link = document.createElement("a");
                        link.href = URL.createObjectURL(blob);
                        link.download = filename;
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);

                        setTimeout(() => {
                            let url = new URL("http://localhost:3000/Admine/Doctor/successDownload");

                            window.location = url;
                        }, 3000);
                    })
                    .catch(error => console.error("Error:", error));
            }

            document.querySelector(".btn1").addEventListener("click", () => {
                downloadFile("http://localhost:3000/Admine/Doctor/DownloadList", `${department}.pdf`);
            });
        });
        ///////////////////////////////////////////////////////
        document.getElementById("downloadWord").addEventListener("click", function () {
            const url = window.location.href;
            const params = new URLSearchParams(new URL(url).search);
            const department = params.get("sub")
            fetch("http://localhost:3000/Admine/Doctor/DownloadList", {
                method: "PATCH",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({ department: department }) // غيّر اسم القسم حسب الحاجة
            })
                .then(response => response.blob())
                .then(blob => {
                    const url = window.URL.createObjectURL(blob);
                    const a = document.createElement("a");
                    a.href = url;
                    a.download = `${department}.docx`;
                    document.body.appendChild(a);
                    a.click();
                    a.remove();
                    setTimeout(() => {
                        let url = new URL("http://localhost:3000/Admine/Doctor/successDownload");

                        window.location = url;
                    }, 3000);
                })
                .catch(error => console.error("Error downloading file:", error));
        });

    </script>
</body>

</html>