<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Archive Ready for Download</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #0044cc, #00aaff);
            font-family: Arial, sans-serif;
            color: white;
            text-align: center;
            overflow: hidden;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            animation: fadeIn 1.5s ease-in-out;
        }
        h1 {
            font-size: 28px;
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }
        input {
            padding: 10px;
            border: none;
            border-radius: 5px;
            width: 80%;
            font-size: 16px;
        }
        button {
            margin-top: 20px;
            padding: 12px 24px;
            background: white;
            color: #0044cc;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }
        button:hover {
            background: #00aaff;
            color: white;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>The Archive is Ready for Download!</h1>
        <input type="text" id="filename" placeholder="Enter file name...">
        <br>
        <button onclick="downloadFile()">Download</button>
    </div>
    <script>
        function downloadFile() {
            const urlParams = new URLSearchParams(window.location.search);
            
            const fileName = document.getElementById('filename').value.trim();
            if (fileName) {
                const modelName = urlParams.get('modelName');
             const idDoctor= urlParams.get('id');
                setTimeout(() => {
                window.location.href = `http://localhost:3000/Doctor/getPdf?id=${idDoctor}&modelName=${modelName}&pdfName=${fileName}`
            },1000)
            } else {
                alert('Please enter a file name!');
            }
        }
    </script>
</body>
</html>
